using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PatrullaEnemigo : MonoBehaviour
{
   /* [SerializeField]
    float velocidadTranslacion = 5f;

    // Update is called once per frame
    void Update()
    {
        transform.Translate(0.0f, 0.0f,-Time.deltaTime*velocidadTranslacion, Space.World);
        
        if(gameObject.Tag=="Enemy"==(0.0f,0.0f,-5.02)
        {
            transform.Translate(-Time.deltaTime * velocidadTranslacion, 0.0f, 0.0f, Space.World);
        }
    }*/
}
